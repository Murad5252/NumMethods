# ─────────────────────────────────────────────
# Numerical Methods Calculator — environment template
# Copy this file to .env and fill in your values.
# .env is git-ignored and must never be committed.
# ─────────────────────────────────────────────

# App metadata (used for About dialog or build scripts)
APP_NAME=NumericalMethodsCalculator
APP_VERSION=1.0.0

# Build configuration: Debug | Release
BUILD_CONFIG=Debug
